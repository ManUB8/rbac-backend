version: "3.9"

services:
  rbac-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rbac-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./code:/app/code
    restart: unless-stopped